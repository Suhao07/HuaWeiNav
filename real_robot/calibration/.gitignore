# Camera calibration results are hardware-specific deployment assets.  Keep
# them in this workspace and mount them read-only; do not commit them by default.
*.yaml
*.yml
# JSON calibration exports are also robot-specific deployment assets. Keep
# them local/remote under the calibration mount unless a reviewed release
# explicitly vendors one.
*.json
!orin26_d435i_mid360_candidate_20260826.json
